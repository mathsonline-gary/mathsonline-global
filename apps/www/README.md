# www

The public marketing site: the pages a visitor reads before they buy — what MathsOnline is, how it
works, pricing per market, and the links into the purchase flows that `apps/purchase` serves.

**Not implemented.** The directory is a placeholder; there is no application here yet. An earlier
`create-next-app` scaffold was removed because it served nothing.
