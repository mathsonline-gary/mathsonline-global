# Root of the API this app talks to, including the /api/v2 prefix. Not NEXT_PUBLIC_: requests are
# made from the server, and the browser has no business knowing the upstream host.
API_URL=http://localhost/api/v2
